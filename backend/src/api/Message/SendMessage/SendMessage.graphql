type SendMessageResponse {
  ok: Boolean!
  error: String
}

input attachTags {
  name: String!
  color: String
}

type Mutation {
  SendMessage(
    level: String!
    contents: String!
    fileName: String
    apiKey: String!
    tags: [attachTags]
  ): SendMessageResponse!
}
